## v1.7.5 (2026-09-17)
- 新码直推:扫到铝壳上的新成品码(9 位,如 26HK04426,或其二维码链接)时,测试记录直接推到 SOAI 云端,不再经过飞书;
  序号自动取 9 位大写码,手填序号框里填新码也一样
- 旧码照旧:飞书标签链接、260YN#### 这类序号、只有磁芯编号或没有编号的记录,推飞书的逻辑、字段、重试与 1.7.4 完全一致;
  一条记录只走一条路,不双写
- 断网不丢:新码记录先写进本机 cloud_pending.jsonl 再后台上传,断网/断电后开机自动补传;
  云端明确退回的记录另存 cloud_rejected.jsonl(带原因),不再反复重试
- 状态查看:管理页显示「新码直推云端」待传/退回条数与最近成功时间;接口 /api/cloud-upload/status 查看状态,/api/cloud-upload/retry 立即重试
- 撤掉 v1.7.4 之后试做的「磁芯自动流水码」:磁芯编号为空时不再自动发码,照旧由人工填写(新二维码是成品码,与磁芯无关)

## v1.7.4 (2026-09-04)
- 扫码枪模式下序号不用手填:扫标签二维码 → 软件向 ERP 查序号(ERP 用标签库映射直接答)→ 拿到序号自动开测。
  原因:飞书记录分享页 2026-09 起要登录,旧的"爬分享页读序号"全部失败,导致"序号必填"时扫码不启动、只能手填
- 序号识别失败时页面提示原因(标签库里没有这个序列码 / 网络),此时才需要手填后重扫
- 推送线程里的序号兜底解析同样改为 ERP 优先,分享页兜底

## v1.7.3 (2026-09-02)
- 新增 UC2910 系列变压器综合测试仪驱动(uc2910),现有 UC2866XB 流程完全不受影响:
  - `TRIG;:FETCh?` 触发扫描并取回定长二进制结果包(按官方数据格式文档),字节序自动识别
  - 覆盖 Turn/Lx/Lk/Cx/Dcr/Q/Acr/Zx 及相位/平衡/脚位短路判定(判定码含LO/HI方向),条码字段一并解析
  - 结果转为与现有流程兼容的格式,判定/CSV/上传飞书/工位标签全链路复用
  - 用法:ERP 产品参数库「仪器驱动」填 uc2910;产品测试项顺序须与仪器设置文件扫描顺序一致
  - 已知限制:设置文件需在仪器面板载入(待厂家SCPI指令集确认能否远程写入);IDN匹配串/字节序以真机联调为准

## v1.7.2 (2026-08-22)
- 序号从源头上传:推送线程里,序号为空但有扫码序列码时,先反查飞书分享页解析序号再上传。
  此前只推serial_code,序号仅屏显+本地CSV,ERP靠海外服务器事后爬分享页补全,大量失败("序号未补全")

## v1.7.1 (2026-08-21)
- 扣偏校验改为测试页内标签,插在「控制台」和「测试」之间(真正的作业流水线):
  初始化成功自动进入「扣偏校验⚠」标签,校验通过自动切到「测试」;顶部导航的独立页取消
- 标签带状态角标:未通过⚠(琥珀色)/已通过✓;未启用金样的产品不显示该标签

## v1.7.0 (2026-08-21)
- 扣偏校验独立成环节页:初始化成功后自动进入「扣偏校验」页,通过后自动跳转测试页;测试页只保留状态条+入口
- 内嵌图文教程:校验页可展开《UC2866XB 偏差扣除操作说明》(也可新窗口打开)
- 校验记录上传飞书 _扣偏校验记录 表(成败都记:时间/工位/产品/结果/容差/各项误差明细/校验ID)
- 测试记录新增「扣偏校验ID」列:每条测试记录可追溯到它之前的那次扣偏校验

## v1.6.3 (2026-08-21)
- 修复:页面"总判定"横幅把NA(未配置仅展示)项当成Fail,导致后端判PASS(飞书记录也是PASS)但页面显示FAIL。
  现横幅只看真正的Fail项,与后端/飞书记录一致

## v1.6.2 (2026-08-21)
- 页面显示配置版本:产品下拉列表每项标注本地"配置vX";测试页"当前产品"卡片显示运行中配置版本
- 同步参数后热更新:当前产品配置有新版本时直接刷新运行中配置(仪器预设未变时),无需重新初始化;扣偏校验自动要求重做
- 同步结果"已是最新"时也显示各产品本地版本号,便于和ERP比对

## v1.6.1 (2026-08-21)
- 修正发版事故:v1.6.0的更新包错打了旧版二进制(build输出在release\smarton-test-system而publish取release根,两者未同步)。
  本版为正确内容:Q判定修复+扣偏校验(金样)功能,见v1.6.0条目
- build_release.ps1 增加末步:组装完成后同步到 release 根(publish取包处),杜绝再次错包

## v1.6.0 (2026-08-18)
- 修复:配置里删掉的测试项(如Q)不再拖累总判定——未配置项仅展示读数,标"仅展示·不判定",不参与Pass/Fail
- 新增:扣偏校验(金样校验)——产品配置含 golden_sample{enabled,tolerance_pct,items[{test_type,pins,standard_value}]} 时,
  初始化/切产品/同步参数后必须先校验:面板扣除偏差→放标准磁芯→点「扣偏校验测试」,各项误差≤阈值才解锁正式测试
- 校验测量不存CSV不推飞书;测试页顶部常驻校验状态条(绿=已通过带时间,黄=锁定)

# Smarton 变压器测试系统 · 更新日志

版本号显示在软件界面顶部（工位徽章旁），也可访问 `/api/version` 查询。

## v1.4.2（2026-08-14）

### 修复
- **老测试表缺「公司/产线/工段」列导致上传被拒**：v1.3.0 之前建的飞书测试表没有这三列，新版软件上传时飞书报 FieldNameNotFound，数据全部积压在重试队列。现在每次会话首次使用某表时自动核对公共列，缺的自动补上，老表无需人工处理。

## v1.5.1（2026-08-14）

### 新增
- **气隙研磨计算（磁芯检产品专用）**：测试页新增「气隙研磨计算」面板（产品编号含"磁芯检"时显示）。开启后，FAIL 磁芯自动带入本次实测电感（Lx2-3/Lx6-5/Lk2-3/Lk6-5），调用云端算法直接给出研磨建议（哪个柱还要磨多少、要不要换胶带）。当前气隙尺寸可修改并记忆；匝数/目标参数默认按模板填好，可展开微调。也可手动点「计算研磨建议」。

## v1.5.0（2026-08-14）

### 新增
- **软件一键自动更新**：飞书新增 `_软件版本` 表作为更新通道。发版方式：`python scripts/publish_release.py --version X.Y.Z --notes "说明"`。设备每小时自动检查，发现新版本在页面顶部弹出横幅，点「立即更新」自动下载→备份→替换→重启，全程约 30 秒，不碰工位身份/参数/测试数据。config.json 加 `"auto_update": true` 可让该设备在仪器空闲时全自动更新（默认关闭）。
- **测试结果字号调节**：测试页结果区新增「字号：标准/大/特大」按钮，选择记忆在本机，方便车间远距离读数。

## v1.4.1（2026-08-14）

### 修复
- **兼容 `*TRG` 一次性返回「确认字节+CSV+0x9F」的仪器固件**：新固件不再单独发确认字节后另发 CSV 包，而是一包发完并以 0x9F 结尾。旧版软件收到首包后仍傻等第二包，10 秒超时报「已触发但未收到测试数据」。现自动识别两种包格式，新旧固件均兼容。
- **参数同步改为「版本或更新时间任一变化即更新」**：此前只认「远端版本号 > 本地」，导致「撤回后重新下发」（版本重回 v1）时本地判定无变化、显示 0 更新。现在同时比对飞书更新时间，重新下发/回退版本都能正确覆盖本地。

## v1.4.0（2026-08-14）

### 修复
- **兼容部分 UC2866XB 固件的 `*TRG` 确认字节**：实测部分仪器（如 Ver 1.0.0 / T5-526-10756）对 `*TRG` 返回 `!` 后会多发 1 个尾字节（0x9F），旧版严格匹配单字节 `!` 导致误报「仪器未返回确认字节，请检查仪器是否开机并处于远程模式」而无法测试。现改为只认开头 `!`，新旧固件均兼容。

### 配套 ERP 更新（soaipower.com，无需升级软件即生效）
- 工位参数下发：已下发产品支持**撤回**（删除后设备下次同步自动将该产品归档移除）
- 工位参数下发：编辑参数支持修改**配置号**（仪器配置ID）

## v1.3.0（2026-08-13）

### 新增
- **工位注册制**：首次启动填公司/产线/工段，注册到飞书 `_工位` 表；此后每条测试数据自动携带工位标签
- **参数自动同步**：每 5 分钟自动从飞书拉取本工位参数，发现新版本自动更新并通知（另保留手动「同步参数」按钮）
- **软件版本号**：新增 `/api/version` 接口，界面顶部显示工位 + 版本徽章
- **产品参数只读**：本机管理页改为只读，参数统一由 ERP「产品参数库」「工位参数下发」管理（快照下发、版本+1 机制、按工位隔离）

## v1.2.x 及更早

- CSV 增加磁芯编号列（v5 文件名）
- 扫码枪兼容无回车模式
- 等效 N 值误差范围、按产品必填项（序号/磁芯编号）
- 飞书上传断网排队续传、表名冲突修复
